[build-system]
requires = [
    "setuptools>=43.0.0",
    "wheel>=0.36.2",
]
build-backend = "setuptools.build_meta"

[pytest]
pythonpath = "test"
testpaths = "test"

[mypy]
mypy_path = [
    ".",
    "test"
]